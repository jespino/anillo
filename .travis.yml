language: python

sudo: false

python:
  - 3.4
  - 3.3

install:
  - pip install werkzeug coveralls flake8

before_script:
  - flake8 anillo

script: nosetests --with-coverage --cover-package=anillo

after_success: coveralls
